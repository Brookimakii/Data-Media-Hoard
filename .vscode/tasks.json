{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Media Hoard: Run",
            "type": "shell",
            "command": "${command:python.interpreterPath}",
            "args": ["main.py"],
            "options": { "cwd": "${workspaceFolder}" },
            "presentation": { "reveal": "always", "panel": "shared" },
            "problemMatcher": []
        }
    ]
}

